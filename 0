import { createClient } from '@supabase/supabase-js'; import fs from 'fs'; import path from 'path'; import dotenv from 'dotenv'; dotenv.config({ path: 'c:\\Rider App\\.env.local' }); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function check() { const { data, error } = await supabase.from('import_history').select('*').order('timestamp', { ascending: false }).limit(5); if (error) console.error(error); else { data.forEach(d = console.log('Import:', d.timestamp, 'Total:', d.total_rows, 'Success:', d.success_count, 'Skipped:', d.skipped_count, 'Failed:', d.failure_count); if (d.failure_count  console.log('Errors:', JSON.stringify(d.errors, null, 2)); if (d.skipped_count  
